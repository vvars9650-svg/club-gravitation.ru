# V5 TEST backend

Entry point: `handler.handler`. Runtime: Python 3.14 with `ydb==3.31.5`. Required variables: `YDB_ENDPOINT`, `YDB_DATABASE`, `V5_TEST_CONSENT_TEXT_HASH` (must start `TEST-`). Function service account supplies credentials. No fallback to memory exists in runtime; `FakeRepository` is test injection only. Package `__init__.py`, `domain.py`, `service.py`, `repository.py`, `ydb_repository.py`, `factory.py`, `handler.py`, `requirements.txt`; exclude tests, schema, bytecode and secrets. Before STEP 5: manually review/apply schema, set TEST variables, then deploy function and configure gateway. No migration/deployment is performed here.
